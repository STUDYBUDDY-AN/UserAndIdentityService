spring:
  application:
    name: user_identity_service
  datasource:
    url: jdbc:mysql://localhost:3307/user_identity?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Anantha2001@
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Enable HikariCP connection pool (default)
  datasource.hikari:
    maximum-pool-size: 10
    minimum-idle: 2

  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: true
    repair: true

server:
  port: 8081

logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"

  level:
    org.springframework.jdbc.core: DEBUG   # Show SQL logs

  sql:
    init:
      mode: always   # run schema.sql if present

  jdbc:
    template:
      fetch-size: 100

app:
  jwtSecret: TXlTdXBlclNlY3VyZUp3dFNlY3JldEtleUZvckxlYXJuaW5nMTIzIQ==
  jwtExpirationMs: 86400000
  admin:
    email: admin@studybuddy.com
    password: admin123




